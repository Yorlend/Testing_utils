#include <stdio.h>

void prime_devisors(int n)
{
    
}

int main()
{
    int a, b, res;

    if (scanf("%d%d", &a, &b) != 2)
        return 1;
    res = a + b;

    printf("%d\n", res);
    return 0;
}